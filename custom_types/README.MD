# PRISMIC CUSTOM TYPES

Here is a backup of the custom types defined inside Prismic in their JSON format.
The JSON format offers more control over the naming of the fields (like the slices names!) so it is the preferred method to define the data formats.
You can just open the JSON Editor inside Prismic and copy and paste from it.
