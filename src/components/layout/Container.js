/**
 * Render the content inside a responsive centered container
 * @param {JSXElement} props
 */
const Container = ({ children }) => <div className="container">{children}</div>;

export default Container;
